# Hospital Coordination System — Python dependencies
# Versions are pinned for reproducibility and mutual compatibility.
#
# Compatibility matrix (all tested together):
#   fastapi  0.111.x  →  requires starlette ~0.37, pydantic >=2.0
#   pydantic 2.7.x    →  used by fastapi for request/response validation
#   starlette 0.37.x  →  ASGI toolkit used internally by fastapi
#   uvicorn  0.29.x   →  ASGI server; [standard] pulls in httptools & watchfiles
python-3.11.9
fastapi==0.111.1
pydantic==2.7.4
starlette==0.37.2
uvicorn[standard]==0.29.0
