# AarogyaLekha — Environment variables
# Copy this file to .env and fill in real values.

# JWT secret — change this to a long random string in production
JWT_SECRET=aarogyalekha-change-me-in-production

# SMTP configuration for email notifications
# Leave empty to disable email sending (the app will still work)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=

# Default admin seed values (used by seed_admin.py)
ADMIN_EMAIL=admin@aarogyalekha.com
ADMIN_PASSWORD=admin123
